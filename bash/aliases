. ~/.zsh/aliases

# changing directory to code project
function c { cd ~/code/$1; }

# alternative to "rails" command to use templates
function railsapp {
  appname=$1
  shift 1
  rails $appname -m http://github.com/ryanb/rails-templates/raw/master/base.rb $@
}

# misc
alias reload='. ~/.bash_profile'
